# Подключение Ollama к платформе 1С: полное руководство

Подробная инструкция по интеграции Ollama с платформой 1С через различные методы взаимодействия.

## 🔌 Основные способы интеграции

### 1. **REST API интеграция (рекомендуемый способ)**

```bsl
// Модуль интеграции с Ollama через REST API

Перем СерверOllama;
Перем ТокенДоступа;

// Основная процедура инициализации
Процедура ИнициализацияOllama() Экспорт
    СерверOllama = "http://localhost:11434";
    ТокенДоступа = ""; // Для локальной Ollama не требуется
КонецПроцедуры

// Функция для отправки HTTP запросов
Функция ОтправитьHTTPЗапрос(Метод, КонечнаяТочка, Тело = Неопределено) Экспорт
    
    ЗапросHTTP = Новый HTTPЗапрос();
    ЗапросHTTP.АдресРесурса = СерверOllama + КонечнаяТочка;
    ЗапросHTTP.Метод = Метод;
    
    Если Тело <> Неопределено Тогда
        ЗапросHTTP.УстановитьТелоИзСтроки(Тело, "application/json");
    КонецЕсли;
    
    HTTPСоединение = Новый HTTPСоединение("localhost", 11434);
    
    Попытка
        ОтветHTTP = HTTPСоединение.ВызватьHTTPМетод(ЗапросHTTP);
        
        Если ОтветHTTP.КодСостояния = 200 Тогда
            Возврат ОтветHTTP.ПолучитьТелоКакСтроку();
        Иначе
            ВызватьИсключение "Ошибка HTTP: " + ОтветHTTP.КодСостояния + " - " + ОтветHTTP.ПолучитьТелоКакСтроку();
        КонецЕсли;
        
    Исключение
        Сообщить("Ошибка подключения к Ollama: " + ОписаниеОшибки());
        Возврат Неопределено;
    КонецПопытки;
    
КонецФункции

// Генерация текста через Ollama
Функция СгенерироватьТекст(Модель, Промпт, ДополнительныеПараметры = Неопределено) Экспорт
    
    Параметры = Новый Соответствие;
    Параметры.Вставить("model", Модель);
    Параметры.Вставить("prompt", Промпт);
    Параметры.Вставить("stream", Ложь);
    
    Если ДополнительныеПараметры <> Неопределено Тогда
        Параметры.Вставить("options", ДополнительныеПараметры);
    КонецЕсли;
    
    ТелоЗапроса = ЗаписатьJSON(Параметры);
    
    Ответ = ОтправитьHTTPЗапрос("POST", "/api/generate", ТелоЗапроса);
    
    Если Ответ = Неопределено Тогда
        Возврат Неопределено;
    КонецЕсли;
    
    ОтветJSON = ПрочитатьJSON(Ответ);
    Возврат ОтветJSON.response;
    
КонецФункции

// Чат с историей сообщений
Функция ОтправитьСообщениеВЧат(Модель, Сообщения, Параметры = Неопределено) Экспорт
    
    Запрос = Новый Соответствие;
    Запрос.Вставить("model", Модель);
    Запрос.Вставить("messages", Сообщения);
    Запрос.Вставить("stream", Ложь);
    
    Если Параметры <> Неопределено Тогда
        Запрос.Вставить("options", Параметры);
    КонецЕсли;
    
    ТелоЗапроса = ЗаписатьJSON(Запрос);
    
    Ответ = ОтправитьHTTPЗапрос("POST", "/api/chat", ТелоЗапроса);
    
    Если Ответ = Неопределено Тогда
        Возврат Неопределено;
    КонецЕсли;
    
    ОтветJSON = ПрочитатьJSON(Ответ);
    Возврат ОтветJSON.message.content;
    
КонецФункции

// Получение списка доступных моделей
Функция ПолучитьСписокМоделей() Экспорт
    
    Ответ = ОтправитьHTTPЗапрос("GET", "/api/tags");
    
    Если Ответ = Неопределено Тогда
        Возврат Новый Массив;
    КонецЕсли;
    
    ОтветJSON = ПрочитатьJSON(Ответ);
    Модели = Новый Массив;
    
    Для Каждого Модель Из ОтветJSON.models Цикл
        Модели.Добавить(Модель.name);
    КонецЦикла;
    
    Возврат Модели;
    
КонецФункции

// Проверка здоровья сервера Ollama
Функция ПроверитьЗдоровьеСервера() Экспорт
    
    Попытка
        Ответ = ОтправитьHTTPЗапрос("GET", "/api/tags");
        Возврат Ответ <> Неопределено;
    Исключение
        Возврат Ложь;
    КонецПопытки;
    
КонецФункции
```

### 2. **Обертка для COM объекта (Windows)**

```bsl
// Модуль для работы через COM-объект (только Windows)

Перем ОберткаPython;

// Инициализация COM-объекта Python
Процедура ИнициализацияCOMОбъекта() Экспорт
    
    Попытка
        ОберткаPython = Новый COMОбъект("Python.Runtime");
        // Здесь будет код инициализации Python среды
    Исключение
        Сообщить("Ошибка инициализации COM объекта Python");
        ОберткаPython = Неопределено;
    КонецПопытки;
    
КонецПроцедуры

// Альтернатива: использование через внешнюю компоненту
Функция ВызватьВнешнююКомпоненту(Метод, Параметры) Экспорт
    
    // Загрузка внешней компоненты .NET
    Попытка
        ВнешняяКомпонента = ЗагрузитьВнешнююКомпоненту("OllamaIntegration.dll");
        Возврат ВнешняяКомпонента.ВызватьМетод(Метод, Параметры);
    Исключение
        Сообщить("Ошибка загрузки внешней компоненты");
        Возврат Неопределено;
    КонецПопытки;
    
КонецФункции
```

## 🏗️ Практические примеры использования в 1С

### 3. **Обработка для интеллектуального поиска**

```bsl
// Обработка "ИнтеллектуальныйПоиск"

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
    
    // Инициализация подключения к Ollama
    ИнтеграцияOllama.ИнициализацияOllama();
    
    // Загрузка списка моделей
    Модели = ИнтеграцияOllama.ПолучитьСписокМоделей();
    Элементы.ВыборМодели.СписокВыбора = Модели;
    
    Если Модели.Количество() > 0 Тогда
        Элементы.ВыборМодели.Значение = Модели[0];
    КонецЕсли;
    
КонецПроцедуры

&НаСервере
Функция ВыполнитьИнтеллектуальныйПоиск(Запрос, Модель)
    
    Промпт = "
    |Пользователь ищет в системе 1С: """ + Запрос + """
    |
    |Проанализируй запрос и предложи возможные варианты поиска:
    |1. Какие документы или справочники могут быть связаны
    |2. Какие отчеты или обработки могут помочь
    |3. Какие типовые сценарии работы в 1С
    |
    |Ответь на русском языке, будь конкретным.
    ";
    
    Параметры = Новый Соответствие;
    Параметры.Вставить("temperature", 0.3);
    Параметры.Вставить("num_predict", 512);
    
    Ответ = ИнтеграцияOllama.СгенерироватьТекст(Модель, Промпт, Параметры);
    Возврат Ответ;
    
КонецФункции

&НаКлиенте
Процедура Поиск(Команда)
    
    Запрос = Элементы.ПолеПоиска.Значение;
    Модель = Элементы.ВыборМодели.Значение;
    
    Если ПустаяСтрока(Запрос) Тогда
        ПоказатьПредупреждение("Введите текст для поиска");
        Возврат;
    КонецЕсли;
    
    // Показать индикатор выполнения
    Элементы.Индикатор.Видимость = Истина;
    
    // Выполнить асинхронно
    ВыполнитьОбработкуНаСервере(Запрос, Модель);
    
КонецПроцедуры

&НаСервере
Процедура ВыполнитьОбработкуНаСервере(Запрос, Модель)
    
    Попытка
        Результат = ВыполнитьИнтеллектуальныйПоиск(Запрос, Модель);
        Элементы.ПолеРезультата.Значение = Результат;
    Исключение
        Сообщить("Ошибка при выполнении поиска: " + ОписаниеОшибки());
    КонецПопытки;
    
    Элементы.Индикатор.Видимость = Ложь;
    
КонецПроцедуры
```

### 4. **Конфигурация для AI-ассистента в документах**

```bsl
// Общий модуль "AIАссистентДокументов"

// Обработка создания текста для документа
Функция СгенерироватьТекстДокумента(ВидДокумента, Контекст) Экспорт
    
    Модель = "llama3.1:8b"; // Модель по умолчанию
    
    Промпт = "
    |Требуется создать текст для документа 1С.
    |
    |Вид документа: " + ВидДокумента + "
    |Контекст: " + Контекст + "
    |
    |Создай профессиональный текст документа, соответствующий стандартам 1С.
    |Учитывай деловой стиль и специфику бухгалтерских/управленческих документов.
    |Будь кратким, но информативным.
    ";
    
    Параметры = Новый Соответствие;
    Параметры.Вставить("temperature", 0.5);
    Параметры.Вставить("num_predict", 1024);
    
    Возврат ИнтеграцияOllama.СгенерироватьТекст(Модель, Промпт, Параметры);
    
КонецФункции

// Анализ текста документа
Функция ПроанализироватьТекстДокумента(ТекстДокумента, ЦельАнализа) Экспорт
    
    Модель = "deepseek-r1:8b"; // Модель для анализа
    
    Промпт = "
    |Проанализируй текст документа 1С:
    |
    |Текст документа:
    |" + ТекстДокумента + "
    |
    |Цель анализа: " + ЦельАнализа + "
    |
    |Предоставь структурированный анализ с выводами и рекомендациями.
    ";
    
    Параметры = Новый Соответствие;
    Параметры.Вставить("temperature", 0.3);
    Параметры.Вставить("num_predict", 2048);
    
    Возврат ИнтеграцияOllama.СгенерироватьТекст(Модель, Промпт, Параметры);
    
КонецФункции

// Генерация SQL запросов для отчетов
Функция СгенерироватьSQLЗапрос(ОписаниеОтчета, СтруктураБазы) Экспорт
    
    Модель = "codellama:7b"; // Специализированная модель для кода
    
    Промпт = "
    |Сгенерируй SQL запрос для 1С на основе описания:
    |
    |Описание отчета: " + ОписаниеОтчета + "
    |Структура базы: " + СтруктураБазы + "
    |
    |Учитывай особенности SQL в 1С (псевдонимы таблиц, особенности синтаксиса).
    |Верни только SQL код без пояснений.
    ";
    
    Параметры = Новый Соответствие;
    Параметры.Вставить("temperature", 0.2);
    Параметры.Вставить("num_predict", 1024);
    
    Возврат ИнтеграцияOllama.СгенерироватьТекст(Модель, Промпт, Параметры);
    
КонецФункции
```

### 5. **Внешняя обработка для чат-бота**

```bsl
// Обработка "ЧатБот1С"

&НаСервере
Перем ИсторияЧата;

Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
    
    ИнтеграцияOllama.ИнициализацияOllama();
    ИсторияЧата = Новый Массив;
    
    // Добавляем системное сообщение
    СистемноеСообщение = Новый Соответствие;
    СистемноеСообщение.Вставить("role", "system");
    СистемноеСообщение.Вставить("content", "
    |Ты - AI-ассистент в системе 1С. Ты помогаешь пользователям с:
    |1. Вопросами по работе с системой
    |2. Анализом данных
    |3. Созданием отчетов
    |4. Решением технических проблем
    |
    |Отвечай профессионально, но понятно. Используй термины 1С.
    |Будь полезным и точным в своих ответах.
    ");
    
    ИсторияЧата.Добавить(СистемноеСообщение);
    
КонецПроцедуры

&НаСервере
Функция ОтправитьСообщение(ТекстСообщения, Модель)
    
    // Добавляем сообщение пользователя в историю
    СообщениеПользователя = Новый Соответствие;
    СообщениеПользователя.Вставить("role", "user");
    СообщениеПользователя.Вставить("content", ТекстСообщения);
    
    ИсторияЧата.Добавить(СообщениеПользователя);
    
    // Ограничиваем историю (последние 10 сообщений)
    Если ИсторияЧата.Количество() > 11 Тогда // 1 системное + 10 диалога
        НоваяИстория = Новый Массив;
        НоваяИстория.Добавить(ИсторияЧата[0]); // Системное сообщение
        // Добавляем последние 10 сообщений
        Для i = (ИсторияЧата.Количество() - 10) По ИсторияЧата.ВерхняяГраница() Цикл
            НоваяИстория.Добавить(ИсторияЧата[i]);
        КонецЦикла;
        ИсторияЧата = НоваяИстория;
    КонецЕсли;
    
    // Параметры генерации
    Параметры = Новый Соответствие;
    Параметры.Вставить("temperature", 0.7);
    Параметры.Вставить("num_predict", 1024);
    
    // Отправляем запрос
    Ответ = ИнтеграцияOllama.ОтправитьСообщениеВЧат(Модель, ИсторияЧата, Параметры);
    
    // Добавляем ответ ассистента в историю
    Если Ответ <> Неопределено Тогда
        СообщениеАссистента = Новый Соответствие;
        СообщениеАссистента.Вставить("role", "assistant");
        СообщениеАссистента.Вставить("content", Ответ);
        ИсторияЧата.Добавить(СообщениеАссистента);
    КонецЕсли;
    
    Возврат Ответ;
    
КонецФункции

&НаКлиенте
Процедура ОтправитьСообщение(Команда)
    
    Текст = Элементы.ПолеВвода.Значение;
    
    Если ПустаяСтрока(Текст) Тогда
        Возврат;
    КонецЕсли;
    
    // Очищаем поле ввода
    Элементы.ПолеВвода.Значение = "";
    
    // Добавляем сообщение пользователя в историю
    ДобавитьСообщениеВИсторию("Пользователь", Текст);
    
    // Показываем индикатор
    Элементы.Индикатор.Видимость = Истина;
    
    // Отправляем на сервер
    Модель = Элементы.ВыборМодели.Значение;
    ВыполнитьОбработкуНаСервере(Текст, Модель);
    
КонецПроцедуры

&НаСервере
Процедура ВыполнитьОбработкуНаСервере(Текст, Модель)
    
    Попытка
        Ответ = ОтправитьСообщение(Текст, Модель);
        Если Ответ <> Неопределено Тогда
            Элементы.ИсторияЧата.Добавить("Ассистент", Ответ);
        Иначе
            Элементы.ИсторияЧата.Добавить("Система", "Ошибка при получении ответа");
        КонецЕсли;
    Исключение
        Элементы.ИсторияЧата.Добавить("Система", "Ошибка: " + ОписаниеОшибки());
    КонецПопытки;
    
    Элементы.Индикатор.Видимость = Ложь;
    
КонецПроцедуры

&НаКлиенте
Процедура ОчиститьИсторию(Команда)
    
    ИсторияЧата = Новый Массив;
    Элементы.ИсторияЧата.Очистить();
    
    // Восстанавливаем системное сообщение
    ВыполнитьВосстановлениеСистемногоСообщенияНаСервере();
    
    Сообщить("История чата очищена");
    
КонецПроцедуры
```

## 🔧 Интеграция через внешние компоненты

### 6. **.NET обертка для Ollama**

```csharp
// OllamaIntegration.cs - .NET библиотека для интеграции

using System;
using System.Net.Http;
using System.Text;
using System.Text.Json;
using System.Threading.Tasks;

namespace OllamaIntegration
{
    public class OllamaClient
    {
        private readonly HttpClient _httpClient;
        private readonly string _baseUrl;

        public OllamaClient(string baseUrl = "http://localhost:11434")
        {
            _baseUrl = baseUrl;
            _httpClient = new HttpClient();
            _httpClient.Timeout = TimeSpan.FromMinutes(5);
        }

        public class GenerateRequest
        {
            public string model { get; set; }
            public string prompt { get; set; }
            public bool stream { get; set; } = false;
            public Options options { get; set; }
        }

        public class Options
        {
            public double temperature { get; set; } = 0.7;
            public int num_predict { get; set; } = 1024;
            public double top_p { get; set; } = 0.9;
        }

        public class GenerateResponse
        {
            public string response { get; set; }
            public bool done { get; set; }
        }

        public class ChatMessage
        {
            public string role { get; set; }
            public string content { get; set; }
        }

        public class ChatRequest
        {
            public string model { get; set; }
            public ChatMessage[] messages { get; set; }
            public bool stream { get; set; } = false;
            public Options options { get; set; }
        }

        public class ChatResponse
        {
            public ChatMessage message { get; set; }
            public bool done { get; set; }
        }

        public async Task<string> GenerateText(string model, string prompt, Options options = null)
        {
            var request = new GenerateRequest
            {
                model = model,
                prompt = prompt,
                stream = false,
                options = options ?? new Options()
            };

            var json = JsonSerializer.Serialize(request);
            var content = new StringContent(json, Encoding.UTF8, "application/json");

            var response = await _httpClient.PostAsync($"{_baseUrl}/api/generate", content);
            response.EnsureSuccessStatusCode();

            var responseJson = await response.Content.ReadAsStringAsync();
            var result = JsonSerializer.Deserialize<GenerateResponse>(responseJson);

            return result.response;
        }

        public async Task<string> SendChatMessage(string model, ChatMessage[] messages, Options options = null)
        {
            var request = new ChatRequest
            {
                model = model,
                messages = messages,
                stream = false,
                options = options ?? new Options()
            };

            var json = JsonSerializer.Serialize(request);
            var content = new StringContent(json, Encoding.UTF8, "application/json");

            var response = await _httpClient.PostAsync($"{_baseUrl}/api/chat", content);
            response.EnsureSuccessStatusCode();

            var responseJson = await response.Content.ReadAsStringAsync();
            var result = JsonSerializer.Deserialize<ChatResponse>(responseJson);

            return result.message.content;
        }

        public async Task<string[]> GetModels()
        {
            var response = await _httpClient.GetAsync($"{_baseUrl}/api/tags");
            response.EnsureSuccessStatusCode();

            var responseJson = await response.Content.ReadAsStringAsync();
            using var document = JsonDocument.Parse(responseJson);
            
            var models = document.RootElement.GetProperty("models");
            var modelNames = new string[models.GetArrayLength()];
            
            for (int i = 0; i < models.GetArrayLength(); i++)
            {
                modelNames[i] = models[i].GetProperty("name").GetString();
            }

            return modelNames;
        }

        public async Task<bool> CheckHealth()
        {
            try
            {
                var response = await _httpClient.GetAsync($"{_baseUrl}/api/tags");
                return response.IsSuccessStatusCode;
            }
            catch
            {
                return false;
            }
        }
    }
}
```

### 7. **Регистрация .NET компоненты в 1С**

```bsl
// Модуль для работы с .NET компонентой

Перем OllamaNET;

// Инициализация .NET компоненты
Процедура ИнициализацияNETКомпоненты() Экспорт
    
    Попытка
        // Загрузка .NET сборки
        Сборка = ЗагрузитьСборкуИзФайла("OllamaIntegration.dll");
        OllamaNET = Сборка.СоздатьЭкземпляр("OllamaIntegration.OllamaClient");
        Сообщить("✅ .NET компонента Ollama загружена");
    Исключение
        Сообщить("❌ Ошибка загрузки .NET компоненты: " + ОписаниеОшибки());
        OllamaNET = Неопределено;
    КонецПопытки;
    
КонецПроцедуры

// Генерация текста через .NET компоненту
Функция СгенерироватьТекстЧерезNET(Модель, Промпт, Параметры = Неопределено) Экспорт
    
    Если OllamaNET = Неопределено Тогда
        ВызватьИсключение ".NET компонента не инициализирована";
    КонецЕсли;
    
    Попытка
        // Создаем объект параметров
        Если Параметры = Неопределено Тогда
            Параметры = OllamaNET.СоздатьЭкземпляр("Options");
        КонецЕсли;
        
        Возврат OllamaNET.GenerateText(Модель, Промпт, Параметры);
        
    Исключение
        ВызватьИсключение "Ошибка генерации через .NET: " + ОписаниеОшибки();
    КонецПопытки;
    
КонецФункции
```

## ⚙️ Конфигурация и настройки

### 8. **Модуль управления настройками**

```bsl
// Общий модуль "НастройкиOllama"

// Структура для хранения настроек
&НаСервере
Структура НастройкиOllama
    Сервер
    Порт
    МодельПоУмолчанию
    Таймаут
    Температура
    МаксимальнаяДлина
КонецСтруктуры

// Загрузка настроек из хранилища
Функция ЗагрузитьНастройки() Экспорт
    
    Настройки = Новый НастройкиOllama;
    
    Попытка
        Хранилище = ХранилищеНастроек.НастройкиOllama;
        
        Настройки.Сервер = Хранилище.Сервер;
        Настройки.Порт = Хранилище.Порт;
        Настройки.МодельПоУмолчанию = Хранилище.МодельПоУмолчанию;
        Настройки.Таймаут = Хранилище.Таймаут;
        Настройки.Температура = Хранилище.Температура;
        Настройки.МаксимальнаяДлина = Хранилище.МаксимальнаяДлина;
        
    Исключение
        // Значения по умолчанию
        Настройки.Сервер = "localhost";
        Настройки.Порт = 11434;
        Настройки.МодельПоУмолчанию = "llama3.1:8b";
        Настройки.Таймаут = 300;
        Настройки.Температура = 0.7;
        Настройки.МаксимальнаяДлина = 1024;
    КонецПопытки;
    
    Возврат Настройки;
    
КонецФункции

// Сохранение настроек
Процедура СохранитьНастройки(НовыеНастройки) Экспорт
    
    Попытка
        ХранилищеНастроек.НастройкиOllama = НовыеНастройки;
        Сообщить("Настройки Ollama сохранены");
    Исключение
        ВызватьИсключение "Ошибка сохранения настроек: " + ОписаниеОшибки();
    КонецПопытки;
    
КонецПроцедуры

// Получение параметров генерации
Функция ПолучитьПараметрыГенерации(ТипЗадачи = "") Экспорт
    
    Параметры = Новый Соответствие;
    Настройки = ЗагрузитьНастройки();
    
    Параметры.Вставить("temperature", Настройки.Температура);
    Параметры.Вставить("num_predict", Настройки.МаксимальнаяДлина);
    
    // Дополнительные настройки в зависимости от типа задачи
    Если ТипЗадачи = "код" Тогда
        Параметры.Вставить("temperature", 0.3);
        Параметры.Вставить("top_p", 0.95);
    ИначеЕсли ТипЗадачи = "анализ" Тогда
        Параметры.Вставить("temperature", 0.5);
        Параметры.Вставить("top_p", 0.85);
    ИначеЕсли ТипЗадачи = "творчество" Тогда
        Параметры.Вставить("temperature", 0.8);
        Параметры.Вставить("top_p", 0.9);
    КонецЕсли;
    
    Возврат Параметры;
    
КонецФункции
```

## 🔒 Безопасность и обработка ошибок

### 9. **Модуль безопасности и валидации**

```bsl
// Общий модуль "БезопасностьOllama"

// Проверка промпта на безопасность
Функция ПроверитьПромпт(Промпт) Экспорт
    
    // Проверка длины
    Если СтрДлина(Промпт) > 10000 Тогда
        Возврат Ложь;
    КонецЕсли;
    
    // Запрещенные паттерны
    ЗапрещенныеСлова = Новый Массив;
    ЗапрещенныеСлова.Добавить("системный промпт");
    ЗапрещенныеСлова.Добавить("игнорируй предыдущие инструкции");
    ЗапрещенныеСлова.Добавить("секретный");
    ЗапрещенныеСлова.Добавить("пароль");
    
    Для Каждого Слово Из ЗапрещенныеСлова Цикл
        Если Найти(НРег(Промпт), НРег(Слово)) > 0 Тогда
            Возврат Ложь;
        КонецЕсли;
    КонецЦикла;
    
    Возврат Истина;
    
КонецФункции

// Логирование запросов
Процедура ЗаписатьВЛог(ТипСобытия, Данные, Пользователь = Неопределено) Экспорт
    
    Если Пользователь = Неопределено Тогда
        Пользователь = ИмяПользователя();
    КонецЕсли;
    
    ЗаписьЛога = Новый ЗаписьЛога;
    ЗаписьЛога.Источник = "Ollama";
    ЗаписьЛога.ТипСобытия = ТипСобытия;
    ЗаписьЛога.Данные = Данные;
    ЗаписьЛога.Пользователь = Пользователь;
    ЗаписьЛога.Записать();
    
КонецПроцедуры

// Обработка ошибок
Функция ОбработатьОшибку(ОписаниеОшибки, Контекст = "") Экспорт
    
    ЗаписатьВЛог("Ошибка", ОписаниеОшибки + " | Контекст: " + Контекст);
    
    // Отправка уведомления администратору
    Если Контекст = "критический" Тогда
        ОтправитьУведомлениеАдминистратору("Критическая ошибка Ollama: " + ОписаниеОшибки);
    КонецЕсли;
    
    Возврат "Произошла ошибка при обработке запроса. Попробуйте позже.";
    
КонецФункения
```

## 🚀 Примеры использования в типовых сценариях

### 10. **Интеграция с обработкой документов**

```bsl
// В модуле документа "Счет"

&НаКлиенте
Процедура СгенерироватьОснованиеПлатежа(Команда)
    
    Контрагент = Объект.Контрагент;
    Сумма = Объект.СуммаДокумента;
    
    Если Контрагент = Неопределено Тогда
        ПоказатьПредупреждение("Выберите контрагента");
        Возврат;
    КонецЕсли;
    
    Запрос = "
    |Сгенерируй основание платежа для счета контрагенту.
    |
    |Контрагент: " + Контрагент.Наименование + "
    |Сумма: " + Формат(Сумма, "ЧГ=") + "
    |
    |Основание должно быть кратким, ясным и соответствовать деловому стилю.
    ";
    
    Попытка
        Ответ = ИнтеграцияOllama.СгенерироватьТекст(
            НастройкиOllama.ЗагрузитьНастройки().МодельПоУмолчанию,
            Запрос,
            НастройкиOllama.ПолучитьПараметрыГенерации("документ")
        );
        
        Объект.ОснованиеПлатежа = Ответ;
        
    Исключение
        ПоказатьПредупреждение("Ошибка генерации: " + ОписаниеОшибки());
    КонецПопытки;
    
КонецПроцедуры
```

### 11. **Анализ данных в отчетах**

```bsl
// В модуле отчета "АнализПродаж"

&НаСервере
Процедура СгенерироватьАналитику(ДанныеОтчета)
    
    // Преобразуем данные отчета в текст
    ТекстДанных = ПреобразоватьДанныеВТекст(ДанныеОтчета);
    
    Промпт = "
    |Проанализируй данные отчета по продажам:
    |
    |" + ТекстДанных + "
    |
    |Выдели ключевые тенденции, аномалии и дай рекомендации.
    |Будь конкретным и используй бизнес-терминологию.
    ";
    
    Попытка
        Анализ = ИнтеграцияOllama.СгенерироватьТекст(
            "deepseek-r1:8b",
            Промпт,
            НастройкиOllama.ПолучитьПараметрыГенерации("анализ")
        );
        
        // Сохраняем анализ в результаты
        Результаты.АнализAI = Анализ;
        
    Исключение
        Сообщить("Ошибка анализа данных: " + ОписаниеОшибки());
    КонецПопытки;
    
КонецПроцедуры
```

## 📊 Мониторинг и диагностика

### 12. **Панель управления Ollama**

```bsl
// Обработка "ПанельУправленияOllama"

&НаСервере
Процедура ОбновитьСтатус()
    
    Попытка
        // Проверка здоровья
        Здоровье = ИнтеграцияOllama.ПроверитьЗдоровьеСервера();
        Элементы.СтатусСервера.Заголовок = ?(Здоровье, "✅ Онлайн", "❌ Оффлайн");
        
        // Загрузка моделей
        Модели = ИнтеграцияOllama.ПолучитьСписокМоделей();
        Элементы.СписокМоделей.Очистить();
        
        Для Каждого Модель Из Модели Цикл
            Элементы.СписокМоделей.Добавить(Модель);
        КонецЦикла;
        
        // Статистика использования
        Статистика = ПолучитьСтатистикуИспользования();
        Элементы.Статистика.Значение = Статистика;
        
    Исключение
        Элементы.СтатусСервера.Заголовок = "❌ Ошибка подключения";
    КонецПопытки;
    
КонецПроцедуры

&НаСервере
Функция ПолучитьСтатистикуИспользования()
    
    // Запрос к логам для получения статистики
    Запрос = Новый Запрос;
    Запрос.Текст = "
    |ВЫБРАТЬ
    |    COUNT(*) КАК ВсегоЗапросов,
    |    SUM(ВЫБОР КОГДА Успешно THEN 1 ELSE 0 КОНЕЦ) КАК Успешных,
    |    МОДЕЛЬ
    |ИЗ
    |    ЛогOllama
    |ГДЕ
    |    ДАТАВРЕМЯ >= &НачалоПериода
    |СГРУППИРОВАТЬ ПО
    |    МОДЕЛЬ
    ";
    
    Запрос.УстановитьПараметр("НачалоПериода", НачалоДня(ТекущаяДата()));
    
    Результат = Запрос.Выполнить().Выгрузить();
    
    Возврат Результат;
    
КонецФункции
```

## 💡 Рекомендации по развертыванию

### Требования к инфраструктуре:
- **Ollama сервер**: Windows/Linux с минимум 8 ГБ RAM
- **Сетевой доступ**: 1С сервер должен иметь доступ к порту 11434
- **Модели**: Рекомендуется начать с llama3.1:8b или qwen2.5:7b
- **Резервное копирование**: Регулярное сохранение настроек и логов

### Этапы внедрения:
1. **Установка Ollama** на отдельном сервере
2. **Загрузка моделей** через Ollama CLI
3. **Тестирование подключения** из 1С
4. **Разработка интерфейсов** для конкретных задач
5. **Обучение пользователей** работе с AI-функционалом